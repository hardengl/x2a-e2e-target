# d7eb7f30-f958-4089-b72c-3180be15a816.x2a

Ansible project generated by x2a-convertor.

## Roles


### chef_automate_deployment


Deploys Chef Automate and/or Chef Infra Server


**Target platforms:** Ubuntu


Run this role:

```bash
ansible-playbook playbooks/run_chef_automate_deployment.yml -i inventory/hosts.yml
```


**Default variables** (override in inventory or extra vars):

| Variable | Default |
|----------|---------|

| `chef_automate_deploy_automate` | `True` |

| `chef_automate_deploy_chef_server` | `True` |

| `chef_automate_cli_path` | `/tmp` |

| `chef_automate_hostname` | `automate.chef.lab` |

| `chef_automate_username` | `jtonello` |

| `chef_automate_longusername` | `John Tonello` |

| `chef_automate_useremail` | `jtonello@chef.lab` |

| `chef_automate_userpassword` | `password` |

| `chef_automate_orgname` | `lab` |

| `chef_automate_longorgname` | `Chef Lab` |




## Prerequisites

### Collections

Install required Ansible collections:

```bash
ansible-galaxy collection install -r collections/requirements.yml
```


## Inventory

Edit `inventory/hosts.yml` to define your target hosts before running any playbook.

## Quick start

```bash
# Install required collections
ansible-galaxy collection install -r collections/requirements.yml

# Edit inventory
vi inventory/hosts.yml

# Run a playbook
ansible-playbook playbooks/run_<role_name>.yml -i inventory/hosts.yml
```